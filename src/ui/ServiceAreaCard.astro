---
const { image = '', city = '', region = '', description = '', cta = { label: '', href: '' }, class: className = '' } = Astro.props;
---
<div class={["group relative flex flex-col p-7 rounded-2xl shadow-lg border transition-all duration-300 backdrop-blur-lg bg-gradient-to-br from-dark via-dark/80 to-dark/60 border-light/10 hover:shadow-2xl hover:-translate-y-1", className].join(' ')}>
  <div class="flex flex-col items-center mb-4">
    {image && (
      <img src={image} alt={city} class="w-20 h-20 object-cover rounded-full border-4 border-light shadow mb-3" loading="lazy" />
    )}
    <h3 class="text-2xl font-bold text-light mb-1">{city}</h3>
    <div class="text-light text-sm font-semibold mb-2">{region}</div>
    <div class="text-light text-base mb-4 text-center">{description}</div>
  </div>
  <a href={cta.href} class="mt-auto inline-block w-full px-5 py-3 rounded-lg font-semibold text-center shadow-md transition-all duration-300 focus:outline-none focus:ring-2 focus:ring-primary focus:ring-offset-2 bg-primary text-light hover:bg-secondary hover:text-light">{cta.label}</a>
</div> 